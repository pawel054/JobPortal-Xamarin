<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JobPortal.View.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Entry x:Name="searchEntry" Placeholder="Wyszukaj po stanoiwsku" HorizontalTextAlignment="Center"/>
            </StackLayout>
            <Label Text="Najnowsze oferty" HorizontalOptions="Center" FontSize="25" Margin="0,30,0,0"/>
            <Button Text="123" Clicked="testclick"/>
            <CollectionView x:Name="testcol">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding ItemName}" />
                            <Label Text="{Binding ItemDescription}" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>